import React from 'react';
import MapPage from 'modules/MapPage';

const MapPageView = ({match}) => {
    console.log(match);
    return (
        <>
            <MapPage/>
        </>

    )
};

export default MapPageView;